# Empty in normal operation. `photocat-maintenance on` writes a `return 503`
# here and reloads nginx, which stops new sessions starting without stopping
# the container — so an ingestion already in flight has time to commit.
